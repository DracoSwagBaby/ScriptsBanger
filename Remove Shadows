-- Disable global shadows immediately to increase FPS
game:GetService("Lighting").GlobalShadows = false

-- Get the Lighting service
local Lighting = game:GetService("Lighting")

-- List of effect names to remove
local effectsToDelete = {
    "Bloom",
    "Blur",
    "DepthOfField",
    "EffectBlur",
    "FBlur",
    "GunHSBLUR",
    "GunHSCC",
    "HeadHurtEffect",
    "IntroBlur",
    "VomitEffect",
    "EffectBLUR",
    "ColorCorrection",
    "Dead"
}

-- Function to delete effects
local function deleteEffects()
    for _, effectName in ipairs(effectsToDelete) do
        local effect = Lighting:FindFirstChild(effectName)
        if effect then
            effect:Destroy()
        end
    end
end

-- Run the function to delete effects immediately
deleteEffects()
